name: smartscore_v2
description: SmartScore Module F - Score Renderer for music score visualization and hit testing
version: 2.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.24.0

# For pure Dart libraries, this package can be used in both Flutter and pure Dart projects
